1. Manifest is not fully calculated in the WorkfloInstalledPlugins. its half of what i want. visual configs are missing. which is the main thing
2. The plugin type is not correct as per what i selected. The plugin type is selected by the manifest calculator, and the manifest calculator is wrong here.
3. NodeSearch: when searching for something, it should also search the external query, not as a last resort.
4. The installed plugins aren't showing in the categories of NodeSearch.
5. When installed a plugin it stays open in that position with "install button" but the plugin was installed successfully.
6. when closig the NodeSearch and opening the search bar is not cleared.

MAKE THE WORKFLOW SAVE ITS NODES IN THE DATABASE.

7. The manifest should be used to show the view of the installed plugins, and the manifest is in the database of the app.tensorify.io WorkfloInstalledPlugins
8. The manifest should be used to show the NodeSettings sheet.
9. The variableName is enforced by the SDK, so, we can now calculate the variable names automatically up until a dataflow.
10. Should add a custom node "Starter" in a subcanvas, and there can be only one starter.
11. There should be a branch custom node.

12. Copy overthe old transpiler to organize the code that will be exported.

13. Examples:
    - Test Pipeline with creating an AlexNet Pipeline with MNIST Dataset in Pytorch
    - Docs for creating plugin
14. Make that example as the starting workflow.

15. Launch without paywall in beta mode by removing the waitlist.

16. Change the coloing of all purple text to primary-readable. [specially the onboarding]
17. Add PostHog
18. GA4
19. SEO Backlinks
20. Marketing 