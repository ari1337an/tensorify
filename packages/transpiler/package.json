{
  "name": "@tensorify.io/transpiler",
  "version": "0.0.1",
  "private": true,
  "description": "The transpiler of the Tensorify Studio.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "start": "ts-node --require tsconfig-paths/register src/usage.ts"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@types/node": "^20.10.0",
    "typescript": "^5.0.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^14.0.0",
    "@tensorify.io/sdk": "workspace:*",
    "@tensorify.io/plugin-engine": "workspace:*"
  }
}
