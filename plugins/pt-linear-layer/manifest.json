{
  "slug": "@cursor/pt-linear-layer",
  "name": "PyTorch Linear Layer",
  "version": "0.0.1",
  "description": "PyTorch Linear Layer plugin for neural networks",
  "main": "src/index.ts",
  "author": "Cursor AI",
  "license": "MIT",
  "engineVersion": "^0.0.1",
  "tags": ["pytorch", "linear", "layer", "neural-network", "tensorify"],
  "category": "model-layer",
  "dependencies": {
    "torch": "^2.0.0"
  },
  "entryPoints": {
    "generateLinearLayer": {
      "description": "Generate PyTorch Linear layer code from payload",
      "parameters": {
        "inFeatures": {
          "type": "number",
          "required": true,
          "description": "Number of input features"
        },
        "outFeatures": {
          "type": "number", 
          "required": true,
          "description": "Number of output features"
        },
        "bias": {
          "type": "boolean",
          "required": false,
          "description": "Whether to include bias term (default: true)"
        }
      }
    },
    "processPayload": {
      "description": "Process payload data and generate PyTorch Linear layer code",
      "parameters": {
        "payload": {
          "type": "object",
          "required": true,
          "description": "Payload containing layer configuration"
        }
      }
    },
    "PyTorchLinearLayer.getTranslationCode": {
      "description": "Generate code using the class method directly",
      "parameters": {
        "settings": {
          "type": "object",
          "required": true,
          "description": "Layer settings with inFeatures, outFeatures, and optional bias"
        }
      }
    }
  }
} 